package org.example

//TIP To <b>Run</b> code, press <shortcut actionId="Run"/> or
// click the <icon src="AllIcons.Actions.Execute"/> icon in the gutter.
fun main() {
    val numberOrders: Int = 75
    var feedbackText: String = "Cood Order1"
    println(numberOrders)
    println(feedbackText)
   // уволен один var worker: Int = 1999
   // уволен один  println(worker)

        // уволен второй var worker: Int = 1997
   // уволен второй println(worker)
    var worker: Int = 1996
    println(worker)
}